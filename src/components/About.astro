<section id="about" class="about-section">
  <div class="container">
    <div class="about-content reveal">
      <div class="section-header">
        <div class="section-label">Legacy</div>
        <h2 class="section-title">
          Driven by <span class="gradient-text">Purpose</span>
        </h2>
      </div>
      <div class="about-grid">
        <div class="about-text-container">
          <p class="about-text">
            I am a <span class="highlight">Software Engineer</span> based in the digital
            world, driven by the challenge of turning complex problems into simple,
            beautiful, and scalable solutions.
          </p>
          <p class="about-text">
            With years of experience in the industry, I've developed a deep
            appreciation for <span class="highlight">technical excellence</span> and
            a user-centric approach. My mission is to build software that not only
            works perfectly but also feels right.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about-section {
    padding: 10rem 0;
    position: relative;
  }

  .about-content {
    max-width: 900px;
    margin: 0 auto;
    text-align: center;
  }

  .section-header {
    margin-bottom: 4rem;
  }

  .section-label {
    color: var(--accent-primary);
    font-size: 0.85rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    margin-bottom: 1rem;
    font-family: var(--font-heading);
  }

  .section-title {
    font-family: var(--font-heading);
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 800;
    letter-spacing: -0.03em;
    margin-bottom: 1.5rem;
    color: var(--text-primary);
  }

  .about-text-container {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .about-text {
    font-size: 1.25rem;
    line-height: 1.8;
    color: var(--text-secondary);
    max-width: 800px;
    margin: 0 auto;
  }

  .highlight {
    color: var(--text-primary);
    font-weight: 600;
    position: relative;
    display: inline-block;
  }

  .highlight::after {
    content: "";
    position: absolute;
    bottom: 2px;
    left: 0;
    width: 100%;
    height: 8px;
    background: var(--gradient-2);
    z-index: -1;
    border-radius: 4px;
  }

  @media (max-width: 768px) {
    .about-section {
      padding: 6rem 0;
    }
    .about-text {
      font-size: 1.1rem;
    }
  }
</style>
