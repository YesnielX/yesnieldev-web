<section class="hero">
  <div class="container">
    <div class="hero-content">
      <div class="profile-image-container">
        <div class="profile-glow"></div>
        <div class="profile-image-wrapper">
          <div class="profile-image-inner">
            <img
              src="/profile.jpg"
              alt="Yesniel - Software Engineer"
              class="profile-image"
            />
          </div>
        </div>
      </div>
      <div class="hero-text">
        <div class="hero-label reveal" style="animation-delay: 0.1s;">
          ✨ Software Engineer
        </div>
        <h1 class="reveal" style="animation-delay: 0.2s;">
          Crafting Digital<br />
          <span class="gradient-text">Experiences</span>
        </h1>
        <p class="reveal" style="animation-delay: 0.3s;">
          Passionate about creating elegant, efficient, and user-friendly web
          applications. With expertise in building scalable solutions, I bring
          ideas to life through clean and maintainable code.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: calc(100vh - 100px);
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 4rem 0;
  }

  .hero-content {
    max-width: 1100px;
    animation: fadeInUp 1s ease-out;
    display: flex;
    flex-direction: row-reverse;
    align-items: center;
    gap: 4rem;
    text-align: left;
  }

  .hero-text {
    flex: 1;
  }

  .profile-image-container {
    position: relative;
    flex-shrink: 0;
    animation:
      fadeInUp 1s ease-out 0.2s both,
      floatGentle 6s ease-in-out infinite;
  }

  @keyframes floatGentle {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  .profile-image-wrapper {
    position: relative;
    width: 220px;
    height: 220px;
    border-radius: 50%;
    background: linear-gradient(145deg, #f0f0f0, #ffffff);
    padding: 4px;
    box-shadow:
      0 8px 32px rgba(99, 102, 241, 0.15),
      0 2px 8px rgba(0, 0, 0, 0.05);
    transition: var(--transition-smooth);
  }

  .profile-image-wrapper:hover {
    box-shadow:
      0 12px 48px rgba(99, 102, 241, 0.25),
      0 4px 16px rgba(0, 0, 0, 0.1);
    transform: scale(1.05);
  }

  .profile-image-wrapper::before {
    content: "";
    position: absolute;
    inset: 0;
    border-radius: 50%;
    padding: 3px;
    background: var(--gradient-1);
    -webkit-mask:
      linear-gradient(#fff 0 0) content-box,
      linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    opacity: 0.6;
  }

  .profile-image-inner {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    overflow: hidden;
    background: white;
    position: relative;
  }

  .profile-image {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    object-fit: cover;
    display: block;
    transition: var(--transition-smooth);
  }

  .profile-image-wrapper:hover .profile-image {
    transform: scale(1.05);
  }

  .profile-glow {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 280px;
    height: 280px;
    border-radius: 50%;
    background: radial-gradient(
      circle,
      rgba(99, 102, 241, 0.15) 0%,
      transparent 70%
    );
    z-index: -1;
    pointer-events: none;
  }

  .hero-label {
    display: inline-block;
    padding: 0.5rem 1.25rem;
    background: var(--gradient-2);
    border: 1px solid rgba(99, 102, 241, 0.2);
    border-radius: 50px;
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--accent-primary);
    margin-bottom: 2rem;
    transition: var(--transition-smooth);
  }

  .hero h1 {
    font-size: clamp(3rem, 8vw, 5.5rem);
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    letter-spacing: -0.03em;
    color: var(--text-primary);
  }

  .hero p {
    font-size: 1.25rem;
    color: var(--text-secondary);
    max-width: 600px;
    line-height: 1.8;
  }

  @media (max-width: 968px) {
    .hero-content {
      flex-direction: column;
      text-align: center;
      gap: 3rem;
    }
    .hero p {
      margin: 0 auto;
    }
    .profile-image-wrapper {
      width: 180px;
      height: 180px;
    }
    .profile-glow {
      width: 240px;
      height: 240px;
    }
  }

  @media (max-width: 768px) {
    .hero h1 {
      font-size: 2.5rem;
    }
    .hero p {
      font-size: 1.125rem;
    }
    .profile-image-wrapper {
      width: 160px;
      height: 160px;
    }
    .profile-glow {
      width: 200px;
      height: 200px;
    }
  }
</style>
