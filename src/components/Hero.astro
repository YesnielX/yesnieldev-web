<section class="hero">
  <div class="hero-bg">
    <div class="mesh-gradient"></div>
    <div class="noise"></div>
  </div>
  <div class="container">
    <div class="hero-content">
      <div class="hero-text">
        <div class="hero-label-wrapper reveal" style="animation-delay: 0.1s;">
          <div class="hero-label">
            <span class="sparkle">✨</span> Available for new projects
          </div>
        </div>
        <h1 class="reveal" style="animation-delay: 0.2s;">
          Engineering <br />
          <span class="gradient-text glow">Scalable</span>
          <br />
          Digital Systems
        </h1>
        <p class="reveal" style="animation-delay: 0.3s;">
          Solving complex architectural challenges through high-performance
          engineering, clean code, and scalable systems designed for global
          impact.
        </p>
        <div class="hero-actions reveal" style="animation-delay: 0.4s;">
          <a href="#projects" class="btn btn-primary">
            <span>Explore My Work</span>
            <svg
              width="20"
              height="20"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              ><line x1="7" y1="17" x2="17" y2="7"></line><polyline
                points="7 7 17 7 17 17"></polyline></svg
            >
          </a>
          <a href="#contact" class="btn btn-secondary">Get in Touch</a>
        </div>
      </div>

      <div class="profile-section reveal" style="animation-delay: 0.25s;">
        <div class="profile-card-group">
          <div class="profile-card">
            <div class="profile-image-wrapper">
              <img
                src="/profile.jpg"
                alt="Yesniel - Software Engineer"
                class="profile-image"
              />
              <div class="image-overlay"></div>
            </div>
            <div class="profile-details">
              <div class="status-badge">
                <span class="dot"></span>
                <span>Online</span>
              </div>
            </div>
          </div>

          <div class="floating-elements">
            <div class="float-item item-1">
              <svg
                width="24"
                height="24"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                ><path
                  d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"
                ></path></svg
              >
            </div>
            <div class="float-item item-2">
              <svg
                width="24"
                height="24"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                ><polyline points="16 18 22 12 16 6"></polyline><polyline
                  points="8 6 2 12 8 18"></polyline></svg
              >
            </div>
            <div class="float-item item-3">
              <svg
                width="24"
                height="24"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                ><circle cx="12" cy="12" r="10"></circle><line
                  x1="14.31"
                  y1="8"
                  x2="20.05"
                  y2="17.94"></line><line x1="9.69" y1="8" x2="21.17" y2="8"
                ></line><line x1="7.38" y1="12" x2="13.12" y2="2.06"
                ></line><line x1="9.69" y1="16" x2="3.95" y2="6.06"></line><line
                  x1="14.31"
                  y1="16"
                  x2="2.83"
                  y2="16"></line><line x1="16.62" y1="12" x2="10.88" y2="21.94"
                ></line></svg
              >
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    padding: 10rem 0 6rem;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: -1;
  }

  .mesh-gradient {
    position: absolute;
    top: -30%;
    right: -10%;
    width: 70%;
    height: 100%;
    background: radial-gradient(
      circle at 50% 50%,
      rgba(129, 140, 248, 0.15) 0%,
      transparent 70%
    );
    filter: blur(100px);
    animation: pulse 12s ease-in-out infinite;
  }

  @keyframes pulse {
    0%,
    100% {
      transform: scale(1) translate(0, 0);
      opacity: 0.6;
    }
    50% {
      transform: scale(1.1) translate(-20px, 20px);
      opacity: 0.8;
    }
  }

  .noise {
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
    opacity: 0.05;
    pointer-events: none;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: 6rem;
    align-items: center;
  }

  .hero-label-wrapper {
    margin-bottom: 2.5rem;
  }

  .hero-label {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.6rem 1.25rem;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 100px;
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--text-secondary);
    backdrop-filter: blur(10px);
  }

  .sparkle {
    font-size: 1.1rem;
    filter: drop-shadow(0 0 5px var(--accent-primary));
  }

  .hero h1 {
    font-family: var(--font-heading);
    font-size: clamp(3.5rem, 8.5vw, 6.5rem);
    font-weight: 800;
    line-height: 0.95;
    letter-spacing: -0.04em;
    margin-bottom: 2.5rem;
    color: var(--text-primary);
  }

  .gradient-text.glow {
    text-shadow: 0 0 40px rgba(129, 140, 248, 0.3);
  }

  .hero p {
    font-size: 1.25rem;
    color: var(--text-secondary);
    max-width: 560px;
    margin-bottom: 3.5rem;
    line-height: 1.7;
  }

  .hero-actions {
    display: flex;
    gap: 1.5rem;
  }

  .btn {
    padding: 1.1rem 2.2rem;
    border-radius: 100px;
    font-weight: 700;
    text-decoration: none;
    transition: var(--transition-spring);
    font-size: 1.05rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
  }

  .btn-primary {
    background: var(--text-primary);
    color: var(--bg-primary);
  }

  .btn-primary:hover {
    transform: scale(1.05) translateY(-4px);
    box-shadow: 0 15px 30px rgba(0, 0, 0, 0.3);
    background: var(--gradient-1);
    color: white;
  }

  .btn-primary svg {
    transition: transform 0.4s ease;
  }

  .btn-primary:hover svg {
    transform: translate(2px, -2px);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.03);
    color: var(--text-primary);
    border: 1px solid rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
  }

  .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(255, 255, 255, 0.2);
    transform: translateY(-4px);
  }

  /* Profile Card Design */
  .profile-section {
    position: relative;
    z-index: 10;
  }

  .profile-card-group {
    position: relative;
    padding: 2rem;
  }

  .profile-card {
    position: relative;
    width: 340px;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 40px;
    padding: 1.25rem;
    backdrop-filter: blur(30px);
    box-shadow:
      var(--shadow-lg),
      0 0 0 1px rgba(255, 255, 255, 0.05);
    transform: rotate(-2deg);
    transition: var(--transition-spring);
  }

  .profile-card:hover {
    transform: rotate(0deg) scale(1.02);
    border-color: rgba(255, 255, 255, 0.15);
  }

  .profile-image-wrapper {
    position: relative;
    width: 100%;
    aspect-ratio: 1;
    border-radius: 32px;
    overflow: hidden;
    margin-bottom: 1.5rem;
  }

  .profile-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: saturate(1.1) contrast(1.1);
    transition: transform 0.8s ease;
  }

  .profile-card:hover .profile-image {
    transform: scale(1.1);
  }

  .image-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to bottom,
      transparent 50%,
      rgba(10, 10, 12, 0.4)
    );
  }

  .profile-details {
    display: flex;
    justify-content: center;
    padding-bottom: 0.5rem;
  }

  .status-badge {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    background: rgba(34, 197, 94, 0.1);
    border: 1px solid rgba(34, 197, 94, 0.2);
    color: #4ade80;
    padding: 0.5rem 1.25rem;
    border-radius: 100px;
    font-size: 0.85rem;
    font-weight: 700;
  }

  .status-badge .dot {
    width: 8px;
    height: 8px;
    background: currentColor;
    border-radius: 50%;
    box-shadow: 0 0 12px currentColor;
    animation: blink 2s infinite;
  }

  @keyframes blink {
    0%,
    100% {
      opacity: 1;
      transform: scale(1);
    }
    50% {
      opacity: 0.4;
      transform: scale(1.2);
    }
  }

  /* Floating Decorative Items */
  .float-item {
    position: absolute;
    width: 60px;
    height: 60px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 18px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--accent-primary);
    backdrop-filter: blur(10px);
    box-shadow: var(--shadow-md);
    z-index: 15;
    animation: float-alt 8s ease-in-out infinite;
  }

  .item-1 {
    top: -10px;
    left: -20px;
    animation-delay: 0s;
    color: #6366f1;
  }
  .item-2 {
    bottom: 40px;
    right: -30px;
    animation-delay: -2s;
    color: #ec4899;
  }
  .item-3 {
    top: 40%;
    left: -50px;
    animation-delay: -4s;
    color: #8b5cf6;
  }

  @keyframes float-alt {
    0%,
    100% {
      transform: translateY(0) rotate(0deg);
    }
    33% {
      transform: translateY(-20px) rotate(10deg);
    }
    66% {
      transform: translateY(10px) rotate(-10deg);
    }
  }

  @media (max-width: 968px) {
    .hero-content {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 5rem;
    }

    .hero-text {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .hero p {
      margin-inline: auto;
    }

    .profile-card {
      width: 300px;
      margin-inline: auto;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding-top: 8rem;
    }
    .hero h1 {
      font-size: clamp(3rem, 12vw, 4.5rem);
    }
    .hero-actions {
      flex-direction: column;
      width: 100%;
      max-width: 300px;
    }
    .item-3 {
      display: none;
    }
  }
</style>
